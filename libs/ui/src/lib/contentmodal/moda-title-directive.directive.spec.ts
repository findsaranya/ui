import { ModaTitleDirectiveDirective } from './moda-title-directive.directive';

describe('ModaTitleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ModaTitleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
